"""Bounded-context modules (skeleton).

M1 includes ONLY governance-safe contexts: `health` and a `sync` engine prototype operating on
in-memory, synthetic data. It explicitly excludes enrolment, child accounts, assessment, grading,
payments, and safeguarding workflows.
"""
